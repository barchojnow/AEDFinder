# Connect IQ build output
bin/
gen/
*.prg
*.prg.debug.xml
*.iq
*.barrel

# Developer key - never commit!
*.der
developer_key*

# OS junk
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc

# The generated tile set. Deliberately never committed: it is rebuilt
# nightly and deployed straight from the Actions artifact, so committing
# it would grow the repository without bound and make every diff
# useless. See .github/workflows/build-data.yml.
_site/
data/
*.geojson
